<!doctype html>
<meta charset="utf-8">
<title>Replay ! Replay script :<%= script %></title>
<head>
    <% include ../elements/header %>
</head>
<body>
    <!-- Give replay page an channel -->
    <input id="control_port" type="hidden" value="null">
    <input id="c_player" type="hidden" value="<%= script.split('_')[1] %>">
    <input id="n_player" type="hidden" value="<%= script.split('_')[2] %>">
    <input id="room_name" type="hidden" value="<%= script %>">
    <div class="p1_current_card" id="p1_hand">
    <div class="text-center">
        <div class="battle_field" id="arena"></div>
    </div>
    <div class="p2_current_card" id="p2_hand">
<!-- Js library -->
<% include ../elements/gamedev_lib %>
</body>
